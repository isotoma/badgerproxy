socket: /tmp/badgerproxy.sock
pidfile: /tmp/badgerpoxy.pid
resolver_cache: /tmp/badgerproxy.resolvercache

services:
  - listen: tcp:8084
    methods:
        allowed:
          - HEAD
          - POST
          - GET
          - CONECT

    ports:
        allowed:
          - 80
          - 443

    rewriter:
        cls: badgerproxy.proxy:RewritingProxyClient
        height: 20px
        bannerurl: http://www.foo.com/banner.gif


  - listen: tcp:8085
    methods:
        allowed:
          - CONNECT

    ports:
        allowed:
          - 22

